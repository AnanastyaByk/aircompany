package planes;

public class NewPlane extends Plane {
    public NewPlane(String modelOfNewPlane, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(modelOfNewPlane, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}
